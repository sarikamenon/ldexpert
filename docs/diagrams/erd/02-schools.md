# ERD — Schools, Contracts & Leads

> Generated by `make erd`. Edit prose freely — only the marker block is rewritten.

<!-- erd:start -->
```mermaid
erDiagram
    schools {
        bigint id PK
        varchar full_name
        varchar display_name FK
        text address
        varchar state FK
        varchar timezone
        bigint manager_id FK
        varchar contact_first_name
        varchar contact_last_name
        varchar contact_phone
        varchar contact_email
        varchar invoice_email
        varchar school_type
        tinyint is_private_student
        tinyint is_auto_extend
        tinyint non_billable_scheduling
        tinyint allow_weekend_scheduling
        varchar external_emr_name
        varchar status FK
        varchar status_reason
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    school_calendar_events {
        bigint id PK
        bigint school_id FK
        varchar title
        varchar event_type FK
        date start_date FK
        date end_date FK
        tinyint request_makeup
        date reminder_date
        date response_date
        text notes
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    school_contracts {
        bigint id PK
        bigint school_id FK
        date start_date FK
        date end_date
        text notes
        varchar document_path
        varchar document_name
        varchar document_mime_type
        bigint document_size
        varchar status FK
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    school_contract_services {
        bigint id PK
        bigint school_contract_id FK
        bigint service_id FK
        decimal rate
        char rate_type
        decimal no_show_rate
        char no_show_rate_type
        timestamp created_at
        timestamp updated_at
    }
    leads {
        bigint id PK
        varchar first_name
        varchar middle_name
        varchar last_name
        varchar email
        varchar gender
        date date_of_birth
        bigint school_id FK
        varchar grade_level
        varchar parent_guardian_name
        varchar parent_guardian_email
        varchar parent_guardian_phone
        text address
        varchar city
        varchar state
        varchar zip_code
        varchar status FK
        text status_reason
        varchar source FK
        date follow_up_date FK
        text follow_up_notes
        bigint created_by FK
        bigint converted_student_id FK
        timestamp converted_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    lead_notes {
        bigint id PK
        bigint lead_id FK
        bigint author_id FK
        text note
        timestamp created_at FK
        timestamp updated_at
        timestamp deleted_at
    }
    %% lead_notes.author_id -> users (other group)
    leads ||--o{ lead_notes : "lead_id"
    %% leads.converted_student_id -> users (other group)
    %% leads.created_by -> users (other group)
    schools ||--o{ leads : "school_id"
    schools ||--o{ school_calendar_events : "school_id"
    school_contracts ||--o{ school_contract_services : "school_contract_id"
    %% school_contract_services.service_id -> services (other group)
    schools ||--o{ school_contracts : "school_id"
    %% schools.manager_id -> users (other group)
```
<!-- erd:end -->
