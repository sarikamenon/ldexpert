# ERD — Finance & Ledger

> Generated by `make erd`. Edit prose freely — only the marker block is rewritten.

<!-- erd:start -->
```mermaid
erDiagram
    ledger_entries {
        bigint id PK
        varchar ledgerable_type FK
        bigint ledgerable_id
        varchar transaction_type FK
        decimal amount
        decimal balance_after
        timestamp recorded_at
        varchar reference_type FK
        bigint reference_id
        text notes
        bigint recorded_by_id FK
        timestamp created_at FK
        timestamp updated_at
        timestamp deleted_at
    }
    expenses {
        bigint id PK
        bigint expense_category_id FK
        date expense_date FK
        decimal amount
        varchar vendor_payee
        text description
        varchar reference
        varchar source_type FK
        bigint source_id
        bigint created_by_id FK
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    expense_categories {
        bigint id PK
        varchar name
        varchar slug FK
        tinyint is_active FK
        timestamp created_at
        timestamp updated_at
    }
    payment_gateway_logs {
        bigint id PK
        bigint payment_gateway_transaction_id FK
        varchar action FK
        varchar direction
        json payload
        varchar gateway_event_id FK
        timestamp created_at
    }
    payment_gateway_transactions {
        bigint id PK
        bigint invoice_id FK
        varchar gateway FK
        varchar gateway_session_id FK
        text payment_url
        varchar status FK
        decimal amount
        varchar currency
        text error_message
        timestamp expires_at
        timestamp completed_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    %% expenses.created_by_id -> users (other group)
    expense_categories ||--o{ expenses : "expense_category_id"
    %% ledger_entries.recorded_by_id -> users (other group)
    payment_gateway_transactions ||--o{ payment_gateway_logs : "payment_gateway_transaction_id"
    %% payment_gateway_transactions.invoice_id -> invoices (other group)
    %% ledger_entries.ledgerable ~> schools (polymorphic, other group)
    %% ledger_entries.ledgerable ~> users (polymorphic, other group)
```
<!-- erd:end -->

## Non-obvious facts

- **`ledger_entries` has exactly one writer: `LedgerService`.** Sign convention
  lives in `TransactionType::balanceDelta()` — never hardcode +/-.
- **`balance_after` invariant**: for each account ordered by `(recorded_at, id)`,
  every row's stored balance equals the running signed sum. `php artisan
  ledger:verify` audits it; backdated writes trigger a full chain recompute.
- `ledgerable` is polymorphic: `School` = accounts receivable, `User` (therapist)
  = accounts payable.
- Soft-deleted rows are conceptually erased — raw queries (`DB::table`,
  `withTrashed`) will double-count them; always go through the model.
