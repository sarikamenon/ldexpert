# ERD — Scheduling & Make-Up

> Generated by `make erd`. Edit prose freely — only the marker block is rewritten.

<!-- erd:start -->
```mermaid
erDiagram
    schedules {
        bigint id PK
        bigint therapist_id FK
        bigint student_id FK
        bigint ssa_id FK
        bigint service_id FK
        bigint school_id FK
        bigint invoice_id FK
        bigint parent_schedule_id FK
        date schedule_date
        time start_time
        time end_time
        varchar recurrence_type
        date recurrence_end_date
        tinyint is_group FK
        varchar recurring_batch_number FK
        varchar group_batch_number FK
        varchar status FK
        bigint sub_therapist_id FK
        varchar sub_request_status
        varchar billing_status FK
        tinyint is_billable FK
        text notes
        bigint created_by FK
        bigint updated_by FK
        text location_details
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_email_logs {
        bigint id PK
        bigint schedule_id FK
        varchar type
        varchar recipient_email
        text custom_message
        bigint sent_by_id FK
        timestamp sent_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_sub_requests {
        bigint id PK
        bigint schedule_id FK
        bigint requested_by_id FK
        text reason
        varchar status FK
        bigint accepted_by_id FK
        datetime accepted_at
        datetime cancelled_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_sub_request_invitees {
        bigint id PK
        bigint schedule_sub_request_id FK
        bigint therapist_id FK
        varchar status
        datetime responded_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_sub_ssas {
        bigint id PK
        bigint schedule_sub_request_id FK
        bigint schedule_id FK
        bigint ssa_id FK
        bigint sub_therapist_id FK
        bigint student_id FK
        bigint service_id FK
        bigint school_id FK
        date session_date
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_makeup_requests {
        bigint id PK
        bigint school_calendar_event_id FK
        bigint schedule_id FK
        bigint student_id FK
        bigint therapist_id FK
        date event_date
        date reminder_date FK
        date response_date FK
        varchar status
        varchar batch_number FK
        datetime reminder_sent_at
        datetime responded_at
        varchar responded_by_type
        bigint responded_by_user_id FK
        varchar response_source
        varchar reason
        bigint makeup_schedule_id FK
        varchar response_token FK
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_makeup_request_email_logs {
        bigint id PK
        bigint schedule_makeup_request_id FK
        varchar type
        varchar recipient_email FK
        varchar recipient_name
        varchar from_email
        varchar from_name
        varchar subject
        varchar status FK
        datetime sent_at
        datetime failed_at
        text error_message
        json metadata
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedule_makeup_availabilities {
        bigint id PK
        bigint therapist_id FK
        date availability_date
        time start_time
        time end_time
        text notes
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    schedules_timezone_backfill_backup {
        bigint id PK
        bigint therapist_id
        date schedule_date
        time start_time
        time end_time
        varchar therapist_timezone
    }
    schedules ||--o{ schedule_email_logs : "schedule_id"
    %% schedule_email_logs.sent_by_id -> users (other group)
    %% schedule_makeup_availabilities.therapist_id -> users (other group)
    schedule_makeup_requests ||--o{ schedule_makeup_request_email_logs : "schedule_makeup_request_id"
    schedules ||--o{ schedule_makeup_requests : "makeup_schedule_id"
    %% schedule_makeup_requests.responded_by_user_id -> users (other group)
    schedules ||--o{ schedule_makeup_requests : "schedule_id"
    %% schedule_makeup_requests.school_calendar_event_id -> school_calendar_events (other group)
    %% schedule_makeup_requests.student_id -> users (other group)
    %% schedule_makeup_requests.therapist_id -> users (other group)
    schedule_sub_requests ||--o{ schedule_sub_request_invitees : "schedule_sub_request_id"
    %% schedule_sub_request_invitees.therapist_id -> users (other group)
    %% schedule_sub_requests.accepted_by_id -> users (other group)
    %% schedule_sub_requests.requested_by_id -> users (other group)
    schedules ||--o{ schedule_sub_requests : "schedule_id"
    schedules ||--o{ schedule_sub_ssas : "schedule_id"
    schedule_sub_requests ||--o{ schedule_sub_ssas : "schedule_sub_request_id"
    %% schedule_sub_ssas.school_id -> schools (other group)
    %% schedule_sub_ssas.service_id -> services (other group)
    %% schedule_sub_ssas.ssa_id -> service_support_agreements (other group)
    %% schedule_sub_ssas.student_id -> users (other group)
    %% schedule_sub_ssas.sub_therapist_id -> users (other group)
    %% schedules.created_by -> users (other group)
    %% schedules.invoice_id -> invoices (other group)
    schedules ||--o{ schedules : "parent_schedule_id"
    %% schedules.school_id -> schools (other group)
    %% schedules.service_id -> services (other group)
    %% schedules.ssa_id -> service_support_agreements (other group)
    %% schedules.student_id -> users (other group)
    %% schedules.sub_therapist_id -> users (other group)
    %% schedules.therapist_id -> users (other group)
    %% schedules.updated_by -> users (other group)
```
<!-- erd:end -->

## Non-obvious facts

- **`schedules.schedule_date` is the UTC calendar date of `start_time`** — never
  display it directly or TZ-shift it; derive display dates from the paired
  datetime. Filter with UTC ranges via `UserTimezoneService::userDayUtcRange()`.
- A booked make-up **is** the rescheduled `schedules` row — there is no separate
  booking table; `schedule_makeup_requests.makeup_schedule_id` links to it.
- `schedules_timezone_backfill_backup` is the reversibility snapshot from the
  local-time→UTC backfill migration — historical, never written by the app.
- A `schedules` row with a make-up request in `sent`/`requested`/`scheduled`
  cannot be deleted (`ScheduleObserver` guard).
