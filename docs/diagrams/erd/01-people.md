# ERD — People & Profiles

> Generated by `make erd`. Edit prose freely — only the marker block is rewritten.

<!-- erd:start -->
```mermaid
erDiagram
    users {
        bigint id PK
        varchar name
        varchar username FK
        varchar email
        timestamp email_verified_at
        varchar password
        varchar role FK
        varchar status FK
        varchar timezone
        varchar remember_token
        timestamp password_change_prompted_at
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    admin_profiles {
        bigint id PK
        bigint user_id FK
        varchar department
        varchar phone
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    parent_profiles {
        bigint id PK
        bigint user_id FK
        varchar phone
        text address
        varchar emergency_contact
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    student_profiles {
        bigint id PK
        bigint user_id FK
        varchar first_name
        varchar middle_name
        varchar last_name
        varchar id_number
        bigint school_id FK
        varchar timezone
        varchar gender
        text address
        varchar city
        varchar state
        varchar zip_code
        varchar parent_guardian_name
        varchar parent_guardian_email
        varchar parent_guardian_phone
        varchar schedule_email
        varchar parent_guardian_2_name
        varchar parent_guardian_2_email
        varchar parent_guardian_2_phone
        bigint parent_id FK
        date date_of_birth
        varchar grade_level
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    therapist_profiles {
        bigint id PK
        bigint user_id FK
        varchar employee_type
        varchar title
        varchar first_name
        varchar last_name
        varchar personal_email
        varchar phone
        varchar ld_email
        varchar llc_name
        text address
        text comments
        bigint position_id FK
        text default_meeting_location
        varchar state FK
        varchar timezone
        bigint manager_id FK
        smallint max_weekly_hours
        decimal hourly_rate
        date dob
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    positions {
        bigint id PK
        varchar name FK
        varchar status
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    position_service {
        bigint id PK
        bigint position_id FK
        bigint service_id FK
    }
    therapist_student {
        bigint id PK
        bigint therapist_id FK
        bigint student_id FK
        timestamp assigned_at
        varchar status
        timestamp created_at
        timestamp updated_at
        timestamp deleted_at
    }
    student_comments {
        bigint id PK
        bigint student_id FK
        bigint author_id FK
        text comment
        timestamp created_at FK
        timestamp updated_at
        timestamp deleted_at
    }
    student_documents {
        bigint id PK
        varchar documentable_type FK
        bigint documentable_id
        bigint uploaded_by_id FK
        varchar document_type FK
        varchar file_name
        varchar file_path
        varchar mime_type
        bigint file_size
        text description
        timestamp created_at FK
        timestamp updated_at
        timestamp deleted_at
    }
    student_imports {
        bigint id PK
        bigint user_id FK
        varchar type FK
        varchar file_path
        varchar file_name
        int total_rows
        int processed_rows
        varchar status FK
        text error_message
        timestamp started_at
        timestamp completed_at
        timestamp created_at FK
        timestamp updated_at
        timestamp deleted_at
    }
    student_import_rows {
        bigint id PK
        bigint student_import_id FK
        int row_number FK
        varchar status FK
        json raw_data
        bigint student_id FK
        text error_message
        timestamp processed_at
        timestamp created_at
        timestamp updated_at
    }
    users ||--o{ admin_profiles : "user_id"
    users ||--o{ parent_profiles : "user_id"
    positions ||--o{ position_service : "position_id"
    %% position_service.service_id -> services (other group)
    users ||--o{ student_comments : "author_id"
    users ||--o{ student_comments : "student_id"
    users ||--o{ student_documents : "uploaded_by_id"
    users ||--o{ student_import_rows : "student_id"
    student_imports ||--o{ student_import_rows : "student_import_id"
    users ||--o{ student_imports : "user_id"
    users ||--o{ student_profiles : "parent_id"
    %% student_profiles.school_id -> schools (other group)
    users ||--o{ student_profiles : "user_id"
    users ||--o{ therapist_profiles : "manager_id"
    positions ||--o{ therapist_profiles : "position_id"
    users ||--o{ therapist_profiles : "user_id"
    users ||--o{ therapist_student : "student_id"
    users ||--o{ therapist_student : "therapist_id"
    student_profiles ||..o{ student_documents : "documentable"
    %% student_documents.documentable ~> session_logs (polymorphic, other group)
```
<!-- erd:end -->
