# Shell scripts must use LF — they run inside the Linux Docker container,
# where CRLF (\r) breaks bash with "$'\r': command not found" errors.
*.sh text eol=lf

# Windows-native scripts keep CRLF.
*.bat text eol=crlf
*.ps1 text eol=crlf
