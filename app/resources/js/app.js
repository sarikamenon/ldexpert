import './bootstrap';

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// jQuery for forms/AJAX (preferred over vanilla JS in this project)
import $ from 'jquery';
window.$ = $;
window.jQuery = $;

